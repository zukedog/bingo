# Convex setup

Run `npx convex dev`, then call `seed:seed` once from the Convex dashboard. The UI currently includes a complete local demo store; the functions here define the production data model and enforce private idea filtering server-side.
